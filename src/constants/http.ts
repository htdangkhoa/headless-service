export const DEFAULT_REQUEST_TIMEOUT = 60000;

export const enum HttpStatus {
  OK = 200,
  NO_CONTENT = 204,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
  REQUEST_TIMEOUT = 408,
  LOGIN_TIMEOUT = 440,
  INTERNAL_SERVER_ERROR = 500,
}
